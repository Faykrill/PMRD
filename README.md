# PMRD
dvc pull
s3://my-bucket/path

## Отчёт

**Лучшие параметры:**
- n_estimators: 200
- max_depth: None

**Метрики:**
- CV score (5-fold): 0.982
- Test accuracy: 0.942

GridSearchCV не дал прироста на тестовой выборке по сравнению 
с базовой моделью (accuracy остался 0.942). Это объясняется:
1. Маленьким размером тестовой выборки (69 объектов)
2. Тем, что дефолтные параметры RandomForest уже близки к оптимальным 
   для данного датасета
3. Увеличение n_estimators с 100 до 200 не изменило предсказания 
   на тестовой выборке

Однако высокий CV score (0.982) показывает, что модель хорошо 
обобщает на обучающих данных.